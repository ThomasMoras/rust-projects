// use ferris_says::say;
// use std::io::{BufWriter, stdout};

fn main() {
    println!("Hello, world!");

    // let stdout = stdout();
    // let message = String::from("Hi there, let's start learn rust !");
    // let width = message.chars().count();
    // let mut writer = BufWriter::new(stdout.lock());
    // say(&message, width, &mut writer).unwrap();
}
